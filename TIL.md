## 과제 진행 로그
1. uv init
2. uv add pandas
3. source .venv\bin\activate (mac os 기준)
    - .ps1 => windows powershell
    - .bat => windows cmd

4. uv add numpy matplotlib seaborn scikit-learn ipykernel jupyter
    - jupyter는 자체적으로 설치해도 됨. 나는 그냥 한번에 설치함.
    - .toml에 알아서 업데이트됨. 나중에 uv sync로 환경 통일 가능.

5. EDA & 데이터 전처리 파이프라인 구현 (단계 1) 시작 🐌
    - 필요한 툴 불러오기
    - 폰트 깨짐 방지 (추가)
    - 파일 불러오기 (read_csv())
    - 기본적인 확인 시작
        1. head()
        2. info() -> NON-NULL 상에는 누락된 값 없음.
        3. isnull().sum() -> NULL값 없음.
        4. shape

    - 데이터셋 기본 정보 정리 (마크다운) : 각 항목이 어떤 의미인지 확인 및 명시
    - 결측치와 이상치 확인 및 처리
        1. describe() -> 0일 수 없는 항목에 0이 있음 (결측치 의심)/ 최댓값이 너무 큼 (이상치 의심)
            - 임의로 하나의 컬럼만 히스토그램을 그려봄.
            - 그 외 다른 컬럼에 대해서도 mean 과 median의 크기 비교를 통해 간단히 편향된 분포인지, 어느쪽으로 편향되었는지 알 수 있다.
        2. (df == 0).sum() : 값이 0으로 처리된 값의 개수 확인
        3. 0일 수 없는 항목에 대해서 NaN 결측치 처리함.
        4. NaN 처리한 결측치에 대한 처리 판단
            
    | 방법 | 언제 사용? | 대표 상황 | 현재 데이터 |
    |---|---|---|---|
    | **평균(Mean)** | 분포가 비교적 대칭이고 이상치가 적을 때 | 키, 시험점수 등 | △ |
    | **중앙값(Median)** | 치우친 분포·이상치가 있을 때 | 소득, 의료 수치 등 | **⭕ 유력** |
    | **ffill** | 순서/시간이 있고 직전 값이 의미 있을 때 | 시계열 | ❌ |
    | **bfill** | 순서/시간이 있고 다음 값이 의미 있을 때 | 시계열 | ❌ |
    | **선형보간(Interpolation)** | 앞뒤 관측치 사이의 연속적 변화가 의미 있을 때 | 시간별 온도 등 | ❌ |
    
        - 현재 데이터는 이상치가 존재하고 히스토그램을 그려보면 skewed (long-tailed) 분포인 것을 확인할 수 있다. (정규분포 아님. 이상치 있음.) -> 평균을 사용하기에 어려워보임
        - 이러한 경우 이상치의 영향을 비교적 적게 받는 중앙값으로 **통계적 대푯값 대치**가 가능해 보임
        - 그외 ffill/bfill은 시계열 데이터가 아니라 해당되지 않으며, 선형 보간법 또한 연속적 변화의 의미가 있는 데이터에 해당되지 않으므로 고려 대상이 아님.

        5. 결측치(NaN) -> 중앙값으로 대치: df[col].median()
            - 컬럼이 여러개라 for문 사용
            - <주의> 대치 처리 후 반드시 사용하는 데이터프레임에 반영하기 (df['col']=...)
            - <추가> Boxplot을 통해 일부 변수에서 IQR 범위를 벗어나는 이상치 후보를 확인하였다. 다만 해당 값들이 실제 관측 가능한 값일 수 있으며 명백한 입력 오류로 판단할 근거가 부족하므로 임의로 제거하지 않고 분석에 포함하였다.
        
        6. 중복 데이터 확인 및 처리 : duplicated(keep=False)
            - 중복값 없어서 별도 처리 없음
        7. [심화] 상관관계 확인 → Outcome과 관련성이 높은 변수 선정
            - Pearson correlation marix
            - 종속변수(당뇨병; Outcome)와 상관이 높은 순(ascending=False)으로 정렬
            - 생각해보기...
            - 주요 변수 선정
                > Glucose는 Outcome과 가장 높은 양의 상관관계(r=0.493)를 보여 주요 변수로 포함하였다.
                > BMI는 Outcome과 두 번째로 높은 상관관계(r=0.312)를 보였으며 신체적 특성을 확인하기 위해 포함하였다.
                > Age와 Pregnancies 역시 Outcome과 상대적으로 높은 양의 상관관계를 보여 포함하였다.
                > DiabetesPedigreeFunction은 상관계수 자체는 상대적으로 낮지만 가족력과 관련된 특성을 추가적으로 분석하기 위해 포함하였다.

                - SkinThickness
                    > BMI와 SkinThickness는 r=0.543으로 비교적 높은 양의 상관관계를 보였다.
                    > 두 변수가 비만·체지방과 관련된 정보를 일부 공유할 가능성이 있으므로, Outcome과 더 높은 상관관계를 보이는 BMI를 주요 분석 변수로 선정하고 SkinThickness는 분포 분석에서 제외하였다.

                - Insulin
                    > 당대사와 관련된 변수임에도 Outcome과의 Pearson 상관계수는 r=0.204로 상대적으로 낮게 나타났다.
                    > 다만 원자료에서 Insulin의 결측값 비율이 높고 결측값 대치가 이루어졌으므로 상관계수 해석에 주의가 필요하다.

            - 주의
                > 상관계수는 변수 간 선형적 관계를 나타내며 인과관계를 의미하지 않는다.
                > 따라서 상관계수만으로 당뇨병의 원인이나 유형을 판단할 수 없다.

        8. 시각화
        - 주요 변수로 피어슨 상관행렬 계산 후 히트맵으로 
        - 히스토그램으로 주요 변수 분포 시각화
            - <주의> 당뇨병 유무로 그룹으 나눠서 시각화했음. 막대 높이는 의미 없음. 표본 차이가 너무 나서 데이터 분포 확인용으로만 쓸것!
        

6. 머신러닝 모델 학습, 과적합 점검 및 하이퍼파라미터 튜닝 (단계 2) 시작 🐢


7. Hugging Face 사전 학습된 모델을 활용한 뉴스 기사 분류 및 추론 (단계 3) 시작 🐰


---
## 다시 공부한 내용:
### 판다스(Pandas)에서 loc, iloc, 그리고 기본 대괄호 df[] 
- 데이터를 조회하고 선택할 때 사용하는 핵심 도구이지만, 각각 참조 방식과 용도에 명확한 차이가 있습니다.
#### .loc[] (Label-based)
- 개념: 행 이름(Index Label)과 열 이름(Column Name)을 사용하여 데이터에 접근합니다.
- 슬라이싱: df.loc['a':'c']처럼 작성하면 라벨 'a'부터 'c'까지 모두 포함합니다.특징: 인덱스 순서가 뒤섞여 있거나 숫자가 아니어도 이름표만 정확히 알면 안전하게 가져올 수 있습니다. 

#### .iloc[] (Integer Location-based)
- 개념: 파이썬의 기본 0번 인덱스 기반 순번(Integer Position)을 사용해 행과 열을 선택합니다.
- 슬라이싱: df.iloc[0:3]으로 작성하면 0, 1, 2번째 행만 가져오고 마지막 3번 인덱스는 제외합니다.
- 특징: 데이터프레임의 순서(몇 번째 행/열인가)가 중요할 때 유용합니다. 

#### df[] (Basic Indexing)
- 개념: 데이터프레임 대괄호는 기본적으로 컬럼(Column)을 선택하거나 불리언 조건(Boolean Mask)을 통한 행 필터링에 최적화되어 있습니다.
- 사용 예시:컬럼 1개 선택: df['col_name']컬럼 여러 개 선택: df[['col1', 'col2']]
- 조건 필터링: df[df['age'] > 30]
- 주의점: df[0]처럼 행 번호로 직접 접근하려고 하면 순번이 아닌 인덱스 라벨 0을 찾기 때문에 에러가 발생할 수 있어, 행과 열을 동시에 정밀 제어할 때는 loc이나 iloc 권장됩니다.

### Boolean Mask

- 조건식의 결과인 `True / False`를 이용하여 원하는 행만 선택하는 방법
- `True`인 행만 데이터프레임에서 가져온다.

```python
df[df['Outcome'] == 0]
```
→ Outcome이 0인 행만 선택

```python
df[df['Outcome'] == 0]['Glucose']
```

### Histogram의 bins
```
plt.hist(data, bins=15)
```
- bins는 데이터의 값 범위를 몇 개의 구간으로 나눌지 지정한다.
- bins=15 → 전체 값의 범위를 15개 구간으로 나누어 각 구간에 속하는 데이터의 빈도를 표시한다.
- bins가 너무 작으면 분포가 지나치게 단순해지고, 너무 크면 그래프가 지나치게 세분화되어 전체적인 분포를 파악하기 어려울 수 있다.

## 새롭게 공부한 내용:
### 범주형 변수 인코딩 (Categorical Encoding)

- **범주형 변수(Categorical Variable)**: 숫자의 크기보다 특정 **범주(Category)** 를 나타내는 변수
  - 예: 성별, 혈액형, 지역, 당뇨병 여부
- 대부분의 머신러닝 모델은 문자열을 그대로 계산하기 어려우므로, 범주형 데이터를 **숫자 형태로 변환하는 과정**이 필요하다.
- 이를 **범주형 변수 인코딩(Categorical Encoding)** 이라고 한다.

#### 1. Label Encoding
각 범주를 하나의 숫자로 변환한다.
```
Female → 0
Male   → 1
```
```
from sklearn.preprocessing import LabelEncoder

encoder = LabelEncoder()
df['Gender'] = encoder.fit_transform(df['Gender'])
```
#### 2. One-Hot Encoding
각 범주를 별도의 컬럼으로 만들어 0과 1로 표현한다.
```
Color     Red   Blue   Green
Red        1      0      0
Blue       0      1      0
Green      0      0      1
```
```
df = pd.get_dummies(df, columns=['Color'])
```